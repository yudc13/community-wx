import { Text, View } from "@tarojs/components";

const Login = () => {
  return (
    <View>
      <Text className="text-black text-text-primary">Login</Text>
    </View>
  );
};

export default Login;
